import PropTypes from "prop-types";
const SetCategoryList = ({ list }) => {
  console.log(list);
  // const {availability ,category_name, logo } = list;
  return <div> </div>;
};

SetCategoryList.propTypes = {
  list: PropTypes.object,
};

export default SetCategoryList;
